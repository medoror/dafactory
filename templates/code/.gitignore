# Build artifacts — never commit these. `factory run` stages with `git add -A`, so
# anything an agent's build produces must be ignored here (ADR-0009).
/target
node_modules/
dist/
build/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/

# OS / editor cruft
.DS_Store
