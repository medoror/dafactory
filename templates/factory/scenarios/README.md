# Scenarios for `{{app}}`

Held-out scenarios live here. They are owned by the factory and are never visible to
the implementer agent: `factory` runs the implementer with its working directory set
to the code root and an environment that does not expose this factory root.

Each scenario is paired to a backlog intent by id (e.g. `S001` ↔ `B1`) and judges
the app from the outside on observable behavior only — see `../judge.md`.

This file is a placeholder written by `factory init`. Add scenarios alongside it.
